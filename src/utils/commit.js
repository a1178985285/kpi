export default {
    hasQx(value, vm) {       
        return true;


        // if(!vm.$store.state.userInfo) return false;
        // var userInfo = vm.$store.state.userInfo;
        // var a = 0;
        // for (var i = 0; i < userInfo.permissionList.length; i++) {
        //     if (value == userInfo.permissionList[i]) {
        //         a++;
        //     }
        // }
        // if (a > 0) {
        //     return true;
        // } else {
        //     return false;
        // }
    },
}